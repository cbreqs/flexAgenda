rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // --- PROTOTYPING RULES ---
    // These allow ANYONE to read/write. This ensures the app works while you set it up.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}