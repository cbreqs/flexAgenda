rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // --- PROTOTYPING RULES ---
    // Allow anyone to read/write for local prototyping.
    // Remember to secure these before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
